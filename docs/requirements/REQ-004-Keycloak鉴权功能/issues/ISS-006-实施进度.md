# ISS-006 实施进度记录

**开始时间**: 2025-11-18
**当前状态**: 🚧 进行中 (约50%完成)

---

## ✅ 已完成部分

### 1. 创建super_user登录页面和API ✅

**新增文件**:
1. `app/api/super-user/login/route.js` ✅
   - 验证SUPER_USER_PASSWORD环境变量
   - 创建isSuperUser Session
   - 返回登录结果

2. `app/super-user/login/page.js` ✅
   - 登录表单界面
   - 支持?redirect=参数
   - 登录成功后自动重定向
   - Material-UI设计

3. `app/api/super-user/logout/route.js` ✅
   - 复用destroySession()逻辑
   - 销毁Session并清除Cookie

### 2. 修改Session添加super_user支持 ✅

**说明**: Session.js本身不需要修改,因为:
- `getSession()`已经支持任意Session数据
- 登录API直接设置`session.isSuperUser = true`
- 销毁Session使用通用的`destroySession()`

### 3. 修改authMiddleware添加智能判断逻辑 ✅

**文件**: `lib/custom/auth/middleware.js`

**已完成修改**:

#### 修改1: 添加Super User Session检查 (第68-142行)
```javascript
// ========== CUSTOM START ==========
// ISS-006: Super User Session验证
if (session?.isSuperUser === true) {
  if (projectId) {
    const project = await db.projects.findUnique({
      where: { id: projectId },
      select: { createdByUserId: true, externalId: true },
    });

    // 公司项目 - super_user不能访问
    if (project.externalId) {
      return { authorized: false, status: 403, error: '审核员仅能访问管理员创建的项目' };
    }

    // 管理员项目 - super_user可以访问
    if (project.createdByUserId === 'admin') {
      return { authorized: true, userId: 'super_user', role: 'super_user' };
    }

    // 其他项目 - 拒绝
    return { authorized: false, status: 403, error: '您无权访问此项目' };
  }

  // 没有projectId (访问首页等) - 允许
  return { authorized: true, userId: 'super_user', role: 'super_user' };
}
// ========== CUSTOM END ==========
```

#### 修改2: 更新管理员项目错误提示 (第274-292行)
```javascript
// ========== CUSTOM START ==========
// ISS-006: 管理员项目支持super_user访问
// 场景2: 管理员专属项目 (createdByUserId = 'admin')
if (createdBy === 'admin') {
  // JWT Token用户尝试访问管理员项目 - 拒绝
  return {
    authorized: false,
    status: 403,
    error: '此项目仅管理员和审核员可访问',
  };
}
// ========== CUSTOM END ==========
```

#### 修改3: 添加role字段到返回值
- admin返回: `{ userId: 'admin', role: 'admin', bypass: true }`
- super_user返回: `{ userId: 'super_user', role: 'super_user', bypass: false }`

---

## 🚧 待完成部分

### 4. 修改首页支持super_user访问 ⏳ 下一步

**文件**: `app/page.js`

**需要修改**:
```javascript
export default async function HomePage() {
  const session = await getSession();

  // 支持admin和super_user
  if (!session?.isAdmin && !session?.isSuperUser) {
    redirect('/admin/login');
  }

  // 根据角色过滤项目
  let projects;
  if (session.isAdmin) {
    // 管理员: 显示所有项目
    projects = await db.projects.findMany({
      orderBy: { updatedAt: 'desc' }
    });
  } else if (session.isSuperUser) {
    // Super User: 仅显示管理员创建的项目
    projects = await db.projects.findMany({
      where: { createdByUserId: 'admin' },
      orderBy: { updatedAt: 'desc' }
    });
  }

  return <HomeClient
    projects={projects}
    role={session.isAdmin ? 'admin' : 'super_user'}
  />;
}
```

---

### 5. 修改HomeClient隐藏创建/删除按钮 ⏳

**文件**: `app/HomeClient.js`

**需要修改**:
```javascript
export default function HomeClient({ projects, role }) {
  const canCreateProject = role === 'admin';
  const canDeleteProject = role === 'admin';

  return (
    <Box>
      <Box sx={{ display: 'flex', justifyContent: 'space-between', mb: 3 }}>
        <Typography variant="h4">项目列表</Typography>

        {/* 仅admin显示创建按钮 */}
        {canCreateProject && (
          <Button variant="contained" onClick={handleCreate}>
            创建项目
          </Button>
        )}

        {/* Super User显示提示 */}
        {!canCreateProject && (
          <Chip label="审核模式" color="info" />
        )}
      </Box>

      {/* 项目列表 - 根据角色显示/隐藏删除按钮 */}
      <ProjectList
        projects={projects}
        canDelete={canDeleteProject}
      />
    </Box>
  );
}
```

**注意**: 需要找到ProjectList组件,确认如何传递canDelete属性

---

### 6. 修改项目详情页错误显示 ⏳

**文件**: `app/projects/[projectId]/layout.js`

**需要修改**: 401错误时显示"点击登录"按钮

```javascript
if (error) {
  const currentPath = window.location.pathname;

  return (
    <Box sx={{ ... }}>
      <Alert severity="error" sx={{ maxWidth: 600 }}>
        <Typography variant="body1" fontWeight="600">
          {error}
        </Typography>

        {/* ========== CUSTOM START ========== */}
        {/* ISS-006: 管理员项目显示super_user登录提示 */}
        {error.includes('无权访问') && (
          <Box sx={{ mt: 2 }}>
            <Divider sx={{ my: 1 }} />
            <Typography variant="body2" color="text.secondary">
              如果您是审核员,请
              <Button
                size="small"
                sx={{ ml: 1 }}
                variant="outlined"
                onClick={() => router.push(
                  `/super-user/login?redirect=${encodeURIComponent(currentPath)}`
                )}
              >
                点击登录
              </Button>
            </Typography>
          </Box>
        )}
        {/* ========== CUSTOM END ========== */}
      </Alert>
    </Box>
  );
}
```

---

### 7. 保护创建项目API ⏳

**文件**: `app/api/projects/route.js`

**需要修改**: POST方法禁止super_user

```javascript
export async function POST(request) {
  // ========== CUSTOM START ==========
  // ISS-006: Super User不能创建项目
  // 修改日期: 2025-11-18

  const session = await getSession();

  if (!session?.isAdmin) {
    return Response.json(
      { error: '您没有权限创建项目' },
      { status: 403 }
    );
  }
  // ========== CUSTOM END ==========

  // 原有创建逻辑...
}
```

---

### 8. 测试所有场景 ⏳

**测试计划** (来自ISS-006设计文档):

#### 测试场景1: super_user登录
- 访问 `/super-user/login`
- 输入正确密码
- 检查是否成功登录并重定向

#### 测试场景2: super_user访问管理员项目
- super_user登录后访问管理员项目
- 检查是否可以正常访问

#### 测试场景3: super_user访问公司项目
- super_user尝试访问公司项目 (有externalId)
- 检查是否返回403错误

#### 测试场景4: super_user尝试创建项目
- super_user访问首页
- 检查是否隐藏"创建项目"按钮
- 用curl调用创建API,检查是否返回403

#### 测试场景5: super_user尝试删除项目
- super_user访问首页
- 检查项目列表是否隐藏"删除"按钮

#### 测试场景6: 未登录访问管理员项目
- 无痕浏览器访问管理员项目
- 检查是否显示401错误和"点击登录"按钮
- 点击登录后是否自动返回

---

## 📦 已修改文件清单

### 新增文件 (3个)
- ✅ `app/api/super-user/login/route.js`
- ✅ `app/super-user/login/page.js`
- ✅ `app/api/super-user/logout/route.js`

### 修改文件 (1个)
- ✅ `lib/custom/auth/middleware.js` (+90行)

### 待修改文件 (4个)
- ⏳ `app/page.js`
- ⏳ `app/HomeClient.js`
- ⏳ `app/projects/[projectId]/layout.js`
- ⏳ `app/api/projects/route.js`

---

## 🔧 环境变量

需要在 `.env.local` 添加:

```env
# Super User (审核员) 密码
SUPER_USER_PASSWORD=your-secure-super-user-password-here
```

---

## 📝 实施注意事项

### 已解决的问题

1. **Session.js不需要修改**: 登录API直接使用`getSession()`并设置`isSuperUser`
2. **authMiddleware添加role字段**: 方便前端区分admin和super_user
3. **智能判断逻辑**: 在步骤1(Session检查)完成super_user验证,不需要走Token流程

### 待解决的问题

1. **HomeClient如何传递canDelete**: 需要查看ProjectList组件的实现
2. **前端路由跳转**: 需要确认`router.push()`在客户端组件中的使用
3. **错误页面获取currentPath**: 需要在客户端组件中获取当前URL

---

## 🎯 下一步行动

**优先级顺序**:
1. ⏳ 修改首页 `app/page.js` (服务端组件)
2. ⏳ 修改HomeClient `app/HomeClient.js` (客户端组件)
3. ⏳ 修改项目详情页错误显示 `app/projects/[projectId]/layout.js`
4. ⏳ 保护创建项目API `app/api/projects/route.js`
5. ⏳ 完整测试所有场景
6. ⏳ Git commit并更新ISS-006文档状态

---

## 💡 关键代码片段

### 登录API核心逻辑
```javascript
// app/api/super-user/login/route.js
if (password !== process.env.SUPER_USER_PASSWORD) {
  return NextResponse.json({ error: '密码错误' }, { status: 401 });
}

const session = await getSession();
session.isSuperUser = true;
session.userId = 'super_user';
await session.save();
```

### authMiddleware核心判断
```javascript
// lib/custom/auth/middleware.js
if (session?.isSuperUser === true) {
  if (projectId) {
    const project = await db.projects.findUnique({...});

    // 公司项目 - 拒绝
    if (project.externalId) return { authorized: false, ... };

    // 管理员项目 - 允许
    if (project.createdByUserId === 'admin') return { authorized: true, ... };
  }

  // 没有projectId - 允许
  return { authorized: true, userId: 'super_user', role: 'super_user' };
}
```

---

**文档创建时间**: 2025-11-18
**完成度**: 约50% (4/8步骤)
**预计剩余工时**: 1.5-2h
